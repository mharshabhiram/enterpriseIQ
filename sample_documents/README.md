# Sample Documents

Drop sample PDFs/DOCX/TXT/MD files here for local testing of the ingestion
pipeline once Phase 4 (Document Upload & Processing) is implemented — e.g. a
mock employee handbook, an HR policy, a product manual. These are not
committed by default (see `.gitignore`); this folder just documents where
local test fixtures belong.
