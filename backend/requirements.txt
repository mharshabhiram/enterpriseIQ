# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pgvector==0.3.4

# --- Validation / config ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Auth / security ---
# NOTE: passlib (a common alternative here) is unmaintained and its bcrypt
# handler crashes with bcrypt>=4.1 (AttributeError on bcrypt.__about__,
# plus incorrect 72-byte-limit handling) - confirmed while building this
# project. Using the bcrypt library directly avoids that entirely.
bcrypt==4.2.0
python-jose[cryptography]==3.3.0

# --- Document processing (Phase 4) ---
pymupdf==1.24.10
python-docx==1.1.2

# --- AI provider layer (Phase 5-6) ---
httpx==0.27.2
tenacity==9.0.0

# --- Utilities ---
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
